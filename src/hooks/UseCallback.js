import { useCallback } from "react";

const UseCallback = () =>{
  return (
    <div>
      <p>useCallback Hook</p>
      <p>This hook lets you cache a function definition between re-renders.</p>
    </div>
  )
}

export default UseCallback;